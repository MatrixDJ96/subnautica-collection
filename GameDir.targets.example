<?xml version="1.0" encoding="utf-8"?>

<!-- INSTRUCTIONS FOR AUTOMATIC FILE COPYING: -->

<!-- 1. Duplicate this GameDir.targets.example file -->
<!-- 2. Rename the new file to "GameDir.targets" (just remove the .example) -->
<!-- 3. Modify the new file's GameDir property so that it matches the path of your Subnautica folder -->

<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <PropertyGroup Condition="'$(Configuration)' == 'SN.STABLE' Or '$(Configuration)' == 'SN.MULTI'">
        <GameDir>C:\Program Files (x86)\Steam\steamapps\common\Subnautica</GameDir>
        <ManagedDir>$(GameDir)\Subnautica_Data\Managed</ManagedDir>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'BZ.STABLE' Or '$(Configuration)' == 'BZ.MULTI'">
        <GameDir>C:\Program Files (x86)\Steam\steamapps\common\SubnauticaZero</GameDir>
        <ManagedDir>$(GameDir)\SubnauticaZero_Data\Managed</ManagedDir>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'SN.MULTI'">
        <NitroxDir>C:\Program Files (x86)\Nitrox</NitroxDir>
    </PropertyGroup>

    <PropertyGroup Condition="'$(Configuration)' == 'BZ.MULTI'">
        <NitroxDir>$(APPDATA)\.botbenson\Subnautica Below Zero\Game</NitroxDir>
    </PropertyGroup>
</Project>
